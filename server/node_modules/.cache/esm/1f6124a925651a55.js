let Post;_256‍.x([["createPost",()=>createPost]]);_256‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);

       const createPost= async (req,res)=>{
    const {content}=req.body;
    if(!content.length){
        return res.json({
            error:"content is required"
        })
    };
    try{
            const post = new Post ({content, postedBy:req.user._id})
    }catch(err){
        _256‍.g.console.log(err);
        res.sendStatus(400)
    }
}