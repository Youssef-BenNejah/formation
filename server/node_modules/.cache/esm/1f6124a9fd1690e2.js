let Post;_fd1‍.x([["createPost",()=>createPost]]);_fd1‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);

       const createPost = async (req, res) => {
    const { content } = req.body;
    if (!content.length) {
        return res.json({
            error: "content is required"
        })
    };
    try {
        const post = new Post({ content, postedBy: req.user._id })
        post.save();
        res.json(post);
    } catch (err) {
        _fd1‍.g.console.log(err);
        res.sendStatus(400)
    }
}